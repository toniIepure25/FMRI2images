# Stage 2 env grid: reliability=on, pca=off (probabilistic fixed-K)
_base_: ../stage1/prob_fixed.yaml

experiment:
  name: prob_fixed_env_R1_P0
  tags: [stage2, env, reliability_on, pca_off, probabilistic, fixed_k]

preprocessing:
  enable_reliability_filter: true
  reliability_threshold: 0.1
  enable_pca: false
  pca_k: null
