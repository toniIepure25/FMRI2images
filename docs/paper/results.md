# Results (draft)

This file is intended to contain **generated** tables/plots references.

Primary source of truth:

- `outputs/reports/**` (evaluation outputs)

(Planned) generated artifacts:

- `outputs/paper/` created by `scripts/build_paper_artifacts.py`

## Expected content

- Evaluation summary tables (per subject × strategy).
- Robustness / ablation summaries.
- Example reconstruction grids (qualitative).
