# Stage 2 env grid: reliability=on, pca=on (deterministic baseline)
_base_: ../stage1/det_fixed.yaml

experiment:
  name: det_fixed_env_R1_P1
  tags: [stage2, env, reliability_on, pca_on, deterministic]

preprocessing:
  enable_reliability_filter: true
  reliability_threshold: 0.1
  enable_pca: true
  pca_k: 512
