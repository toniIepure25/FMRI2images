# Stage 2 env grid: reliability=on, pca=off (deterministic baseline)
_base_: ../stage1/det_fixed.yaml

experiment:
  name: det_fixed_env_R1_P0
  tags: [stage2, env, reliability_on, pca_off, deterministic]

preprocessing:
  enable_reliability_filter: true
  reliability_threshold: 0.1
  enable_pca: false
  pca_k: null
