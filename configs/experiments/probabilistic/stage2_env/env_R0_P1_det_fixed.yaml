# Stage 2 env grid: reliability=off, pca=on (deterministic baseline)
_base_: ../stage1/det_fixed.yaml

experiment:
  name: det_fixed_env_R0_P1
  tags: [stage2, env, reliability_off, pca_on, deterministic]

preprocessing:
  enable_reliability_filter: false
  reliability_threshold: 0.0
  enable_pca: true
  pca_k: 512
