# Stage 2 selection ablation: likelihood (main)
_base_: ../stage1/prob_adapt_q.yaml

experiment:
  name: prob_adapt_q_select_likelihood
  tags: [stage2, selection, likelihood]

inference:
  selection_rule: likelihood
