# Stage 2 additional: scalar logvar shared across dimensions
_base_: ../stage1/prob_adapt_q.yaml

experiment:
  name: prob_adapt_q_scalar
  tags: [stage2, uncertainty, scalar, probabilistic]

decoder:
  uncertainty: scalar
  predict_logvar: true
