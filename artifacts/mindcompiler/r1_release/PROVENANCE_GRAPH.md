# Provenance graph (MINDIR)

```
DATA -> PREPROCESSING -> REPRESENTATION -> OPERATOR -> METRICS -> INFERENCE -> CLAIM -> MANUSCRIPT_FIGURE_TABLE
```

Every scientific artifact is traceable backward through this DAG. Predictor-side (DATA..METRICS training) is firewalled from protected Stage-B outcomes; a CLAIM requires the full backward chain plus a falsifier and a replication status.
