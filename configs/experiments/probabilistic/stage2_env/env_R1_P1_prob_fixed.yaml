# Stage 2 env grid: reliability=on, pca=on (probabilistic fixed-K)
_base_: ../stage1/prob_fixed.yaml

experiment:
  name: prob_fixed_env_R1_P1
  tags: [stage2, env, reliability_on, pca_on, probabilistic, fixed_k]

preprocessing:
  enable_reliability_filter: true
  reliability_threshold: 0.1
  enable_pca: true
  pca_k: 512
